---
platform: linux
image_resource:
  type: registry-image
  source: { repository: alpine }

inputs:
  - name: this
  - name: input
  - name: version
    optional: true

outputs:
  - name: output

run:
  path: this/tar/task.sh

params:
  EXCLUDE:
  INCLUDE: .
  TARBALL_EXTENSION:
  TARBALL_NAME:
  VERSION_FILE:

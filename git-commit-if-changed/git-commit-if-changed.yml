---
platform: linux
image_resource:
  type: registry-image
  source: { repository: engineerbetter/pcf-ops }

inputs:
  - name: this
  - name: input

outputs:
  - name: output

run:
  path: this/git-commit-if-changed.sh
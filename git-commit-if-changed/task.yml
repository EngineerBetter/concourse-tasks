---
platform: linux
image_resource:
  type: registry-image
  source: { repository: alpine/git }

inputs:
  - name: this
  - name: input

outputs:
  - name: output

run:
  path: this/git-commit-if-changed/task.sh

params:
  FILES: "--"
  GIT_AUTHOR_NAME:
  GIT_AUTHOR_EMAIL:
  GIT_COMMIT_MESSAGE:
